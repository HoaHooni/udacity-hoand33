version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to ECR...
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 550762782574.dkr.ecr.us-east-1.amazonaws.com
  build:
    commands:
      - echo Building Docker image...
      - cd analytics
      - docker build -t hoand33-ecr .
      - docker tag hoand33-ecr:latest 550762782574.dkr.ecr.us-east-1.amazonaws.com/hoand33-ecr:latest
  post_build:
    commands:
      - echo Pushing image to ECR...
      - docker push 550762782574.dkr.ecr.us-east-1.amazonaws.com/hoand33-ecr:latest